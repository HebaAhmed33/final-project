"""
Rule-Based Evaluation Engine — Rule Configuration Package.

Each module in this package defines evaluation rules for a specific
compliance standard.  Rules are declarative dicts loaded by the
`rule_evaluator` service at runtime.
"""
